# Frontend configuration — copy to `.env.local` and fill in. Do NOT commit it.

# Base URL of the FastAPI backend (read by frontend/lib/api.ts).
# Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
